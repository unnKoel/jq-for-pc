{{#sprites}}
    .{{name}}{
    display: inline-block;
    vertical-align: middle;
    background: url("/img/{{{escaped_image}}}") no-repeat scroll;
    background-position: {{px.offset_x}} {{px.offset_y}};
    width: {{px.width}};
    height: {{px.height}};
    }
{{/sprites}}